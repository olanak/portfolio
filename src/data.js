export const DATA = {
  "hero": {
    "name": "Olana Kenea Lemesa",
    "tagline": "Cloud Security & IAM Engineer",
    "tags": [
      "Cloud Security",
      "AWS security",
      "IAM",
      "DevSecOps",
      "GCP security",
      "DORA/NIS2",
      "ISO 27001",
      "IaC"
    ],
    "statusText": "Available for opportunities in Europe",
    "locationText": "Istanbul → Berlin (Oct 2026)",
    "githubUrl": "https://github.com/olanak",
    "cvUrl": "/Olana-Kenea-Lemesa-CV.pdf"
  },
  "about": {
    "bio": [
      "I've spent my career securing the things people can't afford to have fail.",
      "",
      "It started with electricity — as a junior ICT officer at the Ethiopian Electric Utility, keeping the systems behind a nation's power running. Then money, administering IBM Power Systems and enterprise storage at the **Commercial Bank of Ethiopia**, one of Africa's largest banks, where downtime isn't an inconvenience — it's a headline.",
      "",
      "Power and finance taught me the same lesson from two angles: **resilience isn't a feature you add, it's a discipline you live.**",
      "",
      "Now I'm carrying that discipline into the cloud. As an **Erasmus Mundus scholar** in Applied Cybersecurity across Turkey and Germany, I build security as code — Terraform-based AWS architectures mapped to **DORA, NIS2, and the EU AI Act**, identity systems, and detection engineering — translating dense regulation into infrastructure you can actually deploy and test.",
      "",
      "I bridge two worlds that rarely meet in one person: the engineer who's run critical datacenters, and the strategist who can explain why it matters in plain business language."
    ],
    "stats": [
      {
        "number": "4+",
        "label": "Years in IT"
      },
      {
        "number": "3",
        "label": "Certifications"
      },
      {
        "number": "2",
        "label": "Master's Degrees"
      },
      {
        "number": "40+",
        "label": "Skills"
      }
    ],
    "infos": [
      {
        "icon": "📍",
        "label": "Location",
        "value": "Istanbul → Berlin (Oct 2026)"
      },
      {
        "icon": "🗣️",
        "label": "Languages",
        "value": "Amharic (native), English (C1), Germany (A1-A2)"
      },
      {
        "icon": "🎯",
        "label": "Focus",
        "value": "Cloud Security, IAM, IaC + GRC"
      },
      {
        "icon": "✈️",
        "label": "Status",
        "value": "Open to EU opportunities"
      }
    ]
  },
  "skills": [
    {
      "title": "Cloud Security",
      "icon": "☁️",
      "items": [
        "AWS Security",
        "IAM & Zero Trust",
        "KMS & Encryption",
        "VPC / Network Security",
        "GuardDuty & Security Hub",
        "CloudTrail & Config",
        "API Security",
        "Container Security"
      ]
    },
    {
      "title": "Infrastructure as Code & DevSecOps",
      "icon": "🏗️",
      "items": [
        "Terraform",
        "Docker",
        "Kubernetes",
        "CI/CD Security",
        "Policy as Code (OPA/Rego)",
        "Linux (RHEL, Ubuntu)"
      ]
    },
    {
      "title": "GRC & Compliance",
      "icon": "📋",
      "items": [
        "DORA",
        "NIS2",
        "EU AI Act",
        "ISO 27001",
        "NIST RMF",
        "CIS Benchmarks",
        "Risk Assessment",
        "Audit & Assurance"
      ]
    },
    {
      "title": "Security Operations",
      "icon": "🔍",
      "items": [
        "SIEM / Log Analysis",
        "Detection Engineering (Sigma)",
        "Vulnerability Assessment",
        "Incident Response",
        "Threat Modeling"
      ]
    },
    {
      "title": "Programming & Scripting",
      "icon": "💻",
      "items": [
        "Python",
        "Bash",
        "HCL (Terraform)",
        "YAML",
        "SQL"
      ]
    },
    {
      "title": "Currently Deepening",
      "icon": "🌱",
      "items": [
        "AWS Solutions Architect & Security",
        "Kubernetes Security",
        "German (A2 → B1)"
      ]
    }
  ],
  "experience": [
    {
      "date": "Dec 2023 – Oct 2025",
      "role": "IS Officer — Server Management",
      "company": "Commercial Bank of Ethiopia",
      "location": "Addis Ababa",
      "items": [
        "Administered IBM Power Systems and enterprise storage across primary and standby datacenters at one of Africa's largest banks",
        "Implemented Ansible automation for AIX server provisioning and PowerHA cluster management",
        "Supported Oracle, PostgreSQL, and Exadata databases ensuring high availability for core banking systems",
        "Managed VMware and hyperconverged infrastructure platforms",
        "Conducted performance monitoring, patch management, and security hardening",
        "Started as IS Graduate Trainee (PMS, Career Development & Succession Management) before advancing to Server Management"
      ]
    },
    {
      "date": "Oct 2023 – Jan 2024",
      "role": "Junior System Administrator",
      "company": "Ethiopian Electric Utility",
      "location": "Addis Ababa",
      "items": [
        "Administered computer storage and backup systems within ICT Operation Support",
        "Performed system monitoring, troubleshooting, and performance tuning",
        "Participated in system upgrades, patching, and security hardening"
      ]
    },
    {
      "date": "Jul 2019 – Oct 2023",
      "role": "Junior ICT Officer",
      "company": "Ethiopian Electric Utility",
      "location": "Bonga",
      "items": [
        "Managed IT infrastructure and networking equipment for the District Automation Office",
        "Implemented IT security protocols against malware and unauthorized access",
        "Managed user accounts, permissions, and network security compliance"
      ]
    }
  ],
  "certifications": [
    {
      "year": "2026",
      "name": "APIsec Certified Practitioner",
      "issuer": "APIsec University",
      "badge": "API Security"
    },
    {
      "year": "2026",
      "name": "Associate Cloud Engineer",
      "issuer": "Google Cloud",
      "badge": "Cloud"
    },
    {
      "year": "2025",
      "name": "Certified in Cybersecurity (CC)",
      "issuer": "ISC2",
      "badge": "Cybersecurity"
    }
  ],
  "education": [
    {
      "degree": "MSc in Applied Cybersecurity",
      "school": "Kadir Has University",
      "meta": "Istanbul, Türkiye — 2025 – Present",
      "status": "Current"
    },
    {
      "degree": "MSc in Computer Science — Cyber Security",
      "school": "SRH University of Applied Sciences",
      "meta": "Berlin, Germany — Starting October 2026",
      "status": "Accepted"
    },
    {
      "degree": "MSc in Computer Networking",
      "school": "Jimma University",
      "meta": "Jimma, Ethiopia — 2019 – 2023",
      "status": "Completed"
    },
    {
      "degree": "BSc in Information Science",
      "school": "University of Gondar",
      "meta": "Gondar, Ethiopia — 2015 – 2018",
      "status": "Completed"
    }
  ],
  "projects": [
    {
      "icon": "🔧",
      "title": "NIS2-Compliant AWS Landing Zone",
      "desc": "An open-source AWS Landing Zone built with Terraform, focused on deploying automated security baselines and robust cloud infrastructure controls to meet strict NIS2 & ISO 27001 Annex A regulatory compliance.",
      "categories": [
        "Cloud",
        "IaC",
        "GRC"
      ],
      "problem": "Eighteen months after NIS2 came into force, almost no public Terraform examples map to it by name — regulated EU teams have CIS and NIST references but nothing tying infrastructure to NIS2 Article 21 obligations.",
      "approach": "Modular Terraform (KMS, S3, CloudTrail, AWS Config, VPC, Organizations, Identity Center, GuardDuty, Security Hub), each resource tagged to the NIS2 measure and ISO 27001 control it satisfies. Validated free on LocalStack Pro, with a single cost-controlled real-AWS run for evidence.",
      "outcome": "Auditable control-to-resource traceability, a full CI gauntlet (Checkov, tfsec, Infracost, terraform test) on every PR, and a reproducible landing zone anyone can stand up with one command.",
      "tech": [
        "AWS",
        "NIS2",
        "CloudSecurity",
        "LandingZone",
        "IaC",
        "Terraform",
        "DevSecOps",
        "CI/CD"
      ],
      "link": "https://github.com/olanak/aws-nis2-baseline",
      "seriesId": "nis2-landing-zone"
    }
  ],
  "contact": {
    "text": "I'm actively looking for Cloud Security, IAM, GRC, and IT Risk roles across Europe — particularly in Germany. Whether you have an opportunity, want to collaborate, or just want to talk security, I'd love to hear from you.",
    "email": "olanakenea6@gmail.com",
    "linkedin": "https://www.linkedin.com/in/olana-kenea",
    "github": "https://github.com/olanak"
  },
  "series": [
    {
      "id": "nis2-landing-zone",
      "title": "Building a NIS2-Compliant AWS Landing Zone",
      "subtitle": "A 6-part build journal",
      "excerpt": "An open-source AWS landing zone in Terraform, mapped explicitly to NIS2 Article 21 and ISO 27001:2022 Annex A — built in public, module by module, over six weeks.",
      "tags": [
        "AWS",
        "NIS2",
        "Terraform",
        "IaC",
        "DevSecOps"
      ],
      "date": "2026-06-09",
      "outline": [
        {
          "part": 0,
          "title": "Week 0 — Why I'm Doing This",
          "summary": "The gap NIS2 leaves in public IaC, the project scope, and the six-week plan.",
          "postId": "post-1780998542956"
        },
        {
          "part": 1,
          "title": "Week 1 — Foundations",
          "summary": "KMS and S3 baseline modules, native terraform test, the full CI pipeline, and branch protection.",
          "postId": null
        },
        {
          "part": 2,
          "title": "Week 2 — The Logging Layer",
          "summary": "CloudTrail, an AWS Config conformance pack, VPC Flow Logs, and the dependency-injection refactor.",
          "postId": null
        },
        {
          "part": 3,
          "title": "Week 3 — Identity Foundations",
          "summary": "AWS Organizations, Service Control Policies, and IAM Identity Center with MFA-enforced permission sets.",
          "postId": null
        },
        {
          "part": 4,
          "title": "Week 4 — Detection",
          "summary": "GuardDuty integrated with Security Hub, and EventBridge to SNS routing for high-severity findings.",
          "postId": null
        },
        {
          "part": 5,
          "title": "Week 5 — Documentation as a Deliverable",
          "summary": "The NIS2-to-ISO control mapping document, architecture diagrams, and README polish.",
          "postId": null
        },
        {
          "part": 6,
          "title": "Week 6 — The Real-AWS Validation Run",
          "summary": "A cost-controlled apply on real AWS, evidence capture, clean teardown, and the retrospective.",
          "postId": null
        }
      ]
    }
  ],
  "blogs": [
    {
      "id": "post-1780998542956",
      "seriesId": "nis2-landing-zone",
      "part": 0,
      "title": "Building a NIS2-Compliant AWS Landing Zone — Week 0: Why I'm Doing This",
      "date": "2026-06-09",
      "excerpt": "On 17 October 2024 the EU's NIS2 Directive came into force — yet eighteen months later, almost no public Terraform examples mention it by name. This series fills that gap: an AWS landing zone mapped to NIS2 and ISO 27001, built in public over six weeks.",
      "content": "NIS2 came into force across the EU in October 2024. Companies in banking, healthcare, energy, and telecom now have a legal obligation to implement the security measures in Article 21 — encryption, incident handling, access control, ongoing risk management. The deadline has already passed, and the fines go up to €10M or 2% of global turnover.\n\nBut if you search \"AWS landing zone Terraform\" today, almost nothing mentions NIS2. There's plenty mapped to CIS or NIST. The framework that actually creates legal obligations for EU companies is mostly absent from public infrastructure-as-code.\n\nSo that's what I'm building: an open-source AWS landing zone in Terraform where each module maps to a specific NIS2 Article 21 measure and ISO 27001:2022 control. I'm doing it in public, one module at a time, over six weeks — including the parts that don't go smoothly.\n\n<img src='/blog/week 0.gif' alt='the plan' style='width:100%;border-radius:12px' />\n\n## Three decisions that shape the project\n\n**Two frameworks, mapped properly.** NIS2 is the legal requirement; ISO 27001:2022 is the set of controls EU teams use to show they meet it. I left out CIS and NIST on purpose — I'd rather map two frameworks properly than four superficially. Every resource is tagged with the measure and control it satisfies, so the compliance mapping comes straight from the infrastructure instead of a separate spreadsheet that goes stale.\n\n**LocalStack to build, real AWS to prove it.** I develop and test everything on LocalStack Pro at zero cost, so anyone can clone the repo and run it without an AWS account. Then one cost-controlled run on real AWS in Week 6 captures the evidence an emulator can't produce — real GuardDuty findings, real Security Hub output — for under €15.\n\n**Track the risk instead of hiding it.** A lot of \"secure infrastructure\" demos aim for a scanner dashboard with everything green and every finding suppressed. I don't think that proves much. A real setup has open findings; what matters is that each one was looked at, judged, and recorded with a reason. That record is the part I'd actually show an auditor. The green badge is just what's left over once you've done that honestly.\n\n## Building in public\n\nThe repo is public from the start — rough commits, work-in-progress branches, bot PRs and all. Every significant choice gets a short Architecture Decision Record, and every scanner finding gets a written disposition. This series is the running commentary: six posts, one per week, each picking up where the last left off.\n\nI'm writing it as a build journal, not a set of tutorials, because a finished repo hides the decisions that went into it. You can't tell what was obvious from the start and what took three tries to get right. Going week by week keeps that visible, which is the part I'd have found useful to read myself.\n\n## Next in the series\n\nPost 2 is Week 1: the first two modules — a KMS key and a locked-down S3 bucket, wired together through a clean interface — plus native `terraform test` and a CI pipeline behind branch protection. By the end of that week the repo has two modules, nine tests, seven checks running on every pull request, and a green badge that actually means something.\n\nRepo Link: [github.com/olanak/aws-nis2-baseline](https://github.com/olanak/aws-nis2-baseline)\n\n---",
      "tags": [
        "AWS",
        "NIS2",
        "CloudSecurity",
        "Compliance",
        "LandingZone",
        "IaC",
        "Terraform",
        "DevSecOps",
        "ProjectKickoff"
      ],
      "published": true
    },
    {
      "id": "post-1781381186343",
      "title": "Building a NIS2-Compliant AWS Landing Zone — Week 1: Foundations",
      "date": "2026-06-13",
      "excerpt": "KMS and S3 baseline modules, native terraform test, the full CI pipeline, and branch protection — the groundwork everything else builds on.",
      "content": "*Last week I laid out the plan: an open-source AWS landing zone in Terraform, mapped to NIS2 and ISO 27001, built on LocalStack so it costs nothing to run. This week the foundation goes in.*\n\nBy the end of Week 1 the repo had two Terraform modules, nine tests, seven CI checks on every pull request, and branch protection that refuses any direct push to `main`. None of that is exciting to look at. But it's the part most portfolio projects skip, and it's the first thing I'd check if I were reviewing someone else's repo — so it's where I started.\n\nHere's how it came together, and the one rule that ended up shaping everything after it.\n\n## Start with the rule: track the risk, don't hide it\n\nMost \"secure infrastructure\" demos go for a clean scanner dashboard — every finding suppressed, every badge green, nothing left to look at. I don't think that proves anything. Real infrastructure has open findings. What matters is whether each one was seen, judged, and written down with a reason.\n\nSo before I wrote a single resource, I set a rule for myself: every scanner finding gets one of four labels — fixed, deferred (with a target date), accepted (out of scope, with a written reason), or suppressed (with the reason inline in the code). The little decision log that records those calls is the thing I'd actually hand an auditor. The green badge is just what you get once you've done that honestly.\n\nThat rule is why the project has a decision log at all, and it's why the later weeks stayed calm instead of turning into a scramble to silence warnings.\n\n## Two modules, one clean seam\n\nWeek 1 ships two modules: a KMS key and a secure S3 bucket. The interesting bit isn't either module on its own — it's how they connect.\n\n<img src='/blog/week 1.png' alt='week 1' style='width:100%;border-radius:12px' />\n\nThe KMS module hands out a `key_arn`. The S3 module takes a `kms_key_arn`. The root config wires one into the other:\n\n```hcl\nmodule \"kms_s3_baseline\" {\n  source = \"../../modules/kms\"\n  # ... CMK with rotation, alias, regulatory tags\n}\n\nmodule \"s3_baseline_logs\" {\n  source      = \"../../modules/s3-baseline\"\n  kms_key_arn = module.kms_s3_baseline.key_arn   # this is the seam\n  # ...\n}\n```\n\nThat one line — the KMS key's ARN passed into the bucket — is the whole idea the project rests on. Small modules with clear inputs and outputs, wired together at the root, beat one giant do-everything module. Week 2 leans on this hard, so it was worth getting right early.\n\n## The S3 baseline: secure unless you go out of your way\n\nThe bucket module isn't just \"a bucket.\" It won't come up in an insecure state:\n\n- encrypted with the customer-managed KMS key, not the AWS-managed default\n- all four public-access-block settings turned on\n- a bucket policy that denies any request not using HTTPS\n- versioning on by default\n- lifecycle rules available behind a variable\n\nThe TLS-only deny is worth showing, because it's the kind of control NIS2 expects and most tutorials leave out:\n\n```hcl\n{\n  Sid       = \"DenyInsecureTransport\"\n  Effect    = \"Deny\"\n  Principal = \"*\"\n  Action    = \"s3:*\"\n  Resource  = [bucket_arn, \"${bucket_arn}/*\"]\n  Condition = { Bool = { \"aws:SecureTransport\" = \"false\" } }\n}\n```\n\nA `Principal = \"*\"` inside a Deny is actually the safest possible scope — it denies everyone who isn't on TLS, including future callers I haven't thought of yet. A scanner will flag that wildcard anyway, and that's fine: it goes in the decision log as accepted, with the reason, rather than being \"fixed\" into something weaker just to clear the warning.\n\n## Testing the things you can't see\n\nTwo modules deserve more than \"it applied without errors.\" Native `terraform test` lets you assert against the plan and against a real apply on LocalStack, and there are two kinds of test that matter here.\n\nThe positive ones check the happy path: rotation is on, all four public-access settings are true, encryption uses the key I passed in.\n\nThe negative ones are the ones I care about more — they check that the guardrails actually stop bad input:\n\n```hcl\nrun \"rejects_invalid_bucket_name\" {\n  command = plan\n  variables { bucket_name = \"Invalid_Name_With_Caps\" }\n  expect_failures = [var.bucket_name]\n}\n```\n\nA test that proves your validation blocks a bad name is worth more than three that prove the good name works. Anyone can make the happy path pass.\n\n## The CI pipeline is the real source of truth\n\n\"Works on my machine\" doesn't count. Every check that matters runs in CI on every PR, and branch protection makes it impossible to merge around them:\n\n| Stage | What it does |\n| --- | --- |\n| `terraform fmt` | Formatting, enforced rather than asked for |\n| `tflint` | Catches provider-specific mistakes plain parsing misses |\n| `tfsec` | Security scan — findings go to the decision log |\n| `checkov` | Second security scan, different rules |\n| `terraform test` | The module and integration suites actually run |\n| `infracost` | Posts a cost estimate on the PR |\n| `terraform-docs` | Regenerates module READMEs on merge |\n\n\nThe two security scanners run in soft-fail mode — they report findings but don't block the merge. That's the same track-the-risk idea, just built into the pipeline: the judgment I record in the decision log is the gate, not a scanner's pass/fail exit code.\n\n## What broke\n\nNo foundation goes in cleanly, and mine didn't either. The problems all lived in the gap between \"works locally\" and \"works in CI\":\n\n- `terraform test` wouldn't reliably pick up a separate `provider.tf` in the test directory — the provider config had to go in the test's own wiring file instead.\n- CI runs its containers as a different user than the surrounding steps, so the auto-generated docs couldn't be committed until I sorted out the file ownership.\n- Branch protection broke the docs automation the moment I turned it on — the workflow had been pushing straight to `main`, which protection now (correctly) rejected, so I had to move it to a pull-request flow.\n\nEach of these turned into a short decision record in the repo. Honestly, the failures are the part worth reading — they're where I actually learned something, as opposed to copying a working snippet.\n\n## NIS2 coverage after Week 1\n\n| Resource | NIS2 Art. 21(2) | ISO 27001:2022 |\n| --- | --- | --- |\n| KMS CMK + rotation | (h) cryptography | A.8.24 |\n| S3 SSE-KMS encryption | (h) cryptography | A.8.24 |\n| S3 versioning | (c) business continuity | A.8.13 |\n| TLS-only bucket policy | (h) cryptography | A.8.24 |\n| Public access block | (i) access control | A.8.3 |\n\nFive control mappings from two modules. Every resource carries tags naming the NIS2 measure and ISO control it satisfies, so the compliance mapping comes out of the infrastructure itself instead of living in a separate doc that drifts out of date.\n\n## Where this leaves things\n\nTwo modules, connected through one clean seam. Tests that prove the guardrails bite, not just that the happy path works. A CI pipeline that's the real source of truth, with a decision log instead of a for-show dashboard. A foundation — which is exactly what Week 1 was supposed to be.\n\n*Next week: the logging layer. CloudTrail with log file validation, AWS Config for continuous compliance, and VPC Flow Logs for network audit — plus the moment the project hit its first \"that's a paid feature\" wall, and what I did about it.*\n\nRepo Link: [github.com/olanak/aws-nis2-baseline](https://github.com/olanak/aws-nis2-baseline)\n\n---",
      "tags": [
        "AWS",
        "Terraform",
        "KMS",
        "CI/CD"
      ],
      "published": true,
      "seriesId": "nis2-landing-zone",
      "part": 1
    },
    {
      "id": "post-1781427861201",
      "title": "Building a NIS2-Compliant AWS Landing Zone - The Logging Layer",
      "date": "2026-06-14",
      "excerpt": "Previously in this series: Week 1 poured the foundation — a KMS key and a secure-by-default S3 bucket, composed through a clean interface, with native `terraform test` and a full CI pipeline behind branch protection. This week builds the layer every incident investigation depends on: the logs.",
      "content": "*Last week was the foundation — a KMS key and a locked-down S3 bucket, wired together, with tests and a CI pipeline behind branch protection. This week is about the thing every incident investigation depends on: the logs.*\n\nYou can't investigate what you never recorded. NIS2 Article 21 leans on this in two places — (b) incident handling and (f) checking whether your controls actually work — and neither is possible without solid logging. So Week 2 builds the three kinds of audit trail: API, configuration, and network. It's also the week I hit the first \"wait, that's behind a paywall\" wall, and the week the CI setup got properly tested.\n\n## Three kinds of audit trail\n\n<img src='/blog/week 2.png' alt='week 2' style='width:100%;border-radius:12px' />\n\nThree services, one shared shape: each writes KMS-encrypted logs, CloudTrail and Config both deliver to the same S3 bucket, and CloudTrail and Flow Logs also stream to CloudWatch Logs — which sets up the real-time detection work coming in Week 4.\n\n## The wall: CloudTrail is a paid feature\n\nThe first `terraform apply` of the CloudTrail module failed with a flat HTTP 501: the CloudTrail API wasn't implemented, or was a Pro feature. LocalStack's free tier doesn't emulate it.\n\nMy first instinct was to work around it — add a flag so the module stays plan-only when running locally. Then I checked the GitHub Student Developer Pack and found that LocalStack offers a Student plan with the full Pro feature set, free while you're enrolled. So the \"limitation\" was really just a tier I hadn't claimed yet, and the better tier was sitting there for nothing.\n\nClaiming it instead of coding around it raised the ceiling for the whole project. Everything after this — Config, GuardDuty, Security Hub, Organizations — now applies for real, both locally and in CI, without any environment-specific flags cluttering the code. The lesson I wrote down: get your local environment to match production rather than building workarounds you'll forget to rip out later.\n\n## The refactor: let callers add, never remove\n\nCloudTrail needs to write to an S3 bucket — the same logs bucket the Week 1 module creates. But that bucket already had a locked-down policy, and CloudTrail needs a couple of extra statements layered on top. Two tempting-but-bad options: bake CloudTrail's needs into the generic bucket module, or let callers swap out the whole policy and risk dropping the baseline protections by accident.\n\nThe cleaner answer was to keep the baseline statements fixed and give callers a slot to add to:\n\n```hcl\nlocals {\n  baseline_policy_statements = [\n    # TLS-only deny + encryption-required deny — always there\n  ]\n  all_policy_statements = concat(\n    local.baseline_policy_statements,\n    var.additional_policy_statements,   # callers add; they can't remove\n  )\n}\n```\n\nCallers compose what they need, and the baseline can't be weakened. The payoff showed up a day later: when AWS Config arrived needing its own delivery permissions on the same bucket, wiring it in was a twelve-line `concat` instead of a refactor.\n\n## CloudTrail, past the defaults\n\nThe trail isn't just switched on — it's set up the way an auditor would want:\n\n```hcl\nresource \"aws_cloudtrail\" \"this\" {\n  is_multi_region_trail      = true   # every region, not just the home one\n  enable_log_file_validation = true   # SHA-256 + RSA tamper detection\n  kms_key_id                 = var.kms_key_arn\n  cloud_watch_logs_group_arn = \"${aws_cloudwatch_log_group.trail.arn}:*\"\n}\n```\n\nLog file validation is the part that's easy to overlook. It produces signed digests, so an auditor can prove the logs weren't edited after the fact — which is basically what NIS2 (f), \"assess whether your measures are effective,\" needs in practice. No clause names the feature, but the requirement effectively assumes it. Retention defaults to 365 days, because Article 23's reporting timeline (24-hour early warning, 72-hour notification, one-month final report) means an investigator needs logs going back well before the incident started.\n\n## AWS Config: checking the running resources\n\nCloudTrail records what happened. AWS Config records what is — the actual state of every resource, checked continuously against rules. I set up a recorder, a delivery channel, and six managed rules, each one tied to a specific NIS2 measure:\n\n| Config rule | NIS2 21(2) |\n| --- | --- |\n| `S3_BUCKET_SERVER_SIDE_ENCRYPTION_ENABLED` | (h) cryptography |\n| `S3_BUCKET_PUBLIC_READ_PROHIBITED` | (i) access control |\n| `CLOUD_TRAIL_ENABLED` | (b) incident handling |\n| `CLOUD_TRAIL_LOG_FILE_VALIDATION_ENABLED` | (f) effectiveness |\n| `ENCRYPTED_VOLUMES` | (h) cryptography |\n| `IAM_USER_MFA_ENABLED` | (i) access control |\n\nThe difference that matters: the static scanners (Checkov, tfsec) check my code at PR time, while Config checks the running resources, continuously, every time something changes. Both belong in a real setup. And six separate rules, each carrying its own NIS2 tag, tell a clearer story to an auditor than one bundled conformance pack would — every rule points back to a specific clause.\n\n## VPC Flow Logs, scoped honestly\n\nThe third trail is network traffic: a two-AZ VPC with public and private subnets, NAT for outbound, and Flow Logs capturing all traffic to a KMS-encrypted log group.\n\nOne thing worth being precise about, because overstating it is a quick way to lose a reviewer's trust: flow logs capture metadata, not contents. The 5-tuple — source and destination IP, ports, protocol — plus the action and byte counts. Not the packets themselves. So \"network visibility\" here means I can reconstruct who talked to whom and how much, which is what you need to trace lateral movement or data leaving the network. It does not mean I can read what was sent.\n\nA small trap I nearly fell into: the flow-logs IAM role has to trust `vpc-flow-logs.amazonaws.com`, not `ec2.amazonaws.com`. Flow logs are delivered by the flow-logs service, not by EC2 — get the principal wrong and the log shows up as active while quietly delivering nothing.\n\n<img src='/blog/week 2.gif' alt='week 2' style='width:100%;border-radius:12px' />\n\n## What broke: the CI cascade\n\nThe hard part of Week 2 wasn't the modules — it was the CI plumbing, which had quietly been broken since I turned on branch protection. It came apart in five connected pieces, which I had to unwind one at a time:\n\n- Branch protection blocked the docs bot from pushing to `main`, so I moved the auto-docs workflow to open a pull request instead of pushing directly.\n- A container was running as the wrong user, so the bot couldn't write the generated files — fixed by sorting out the workspace ownership.\n- The docs tool silently skipped a module, because it wasn't parsing the comma-separated directory list the way I assumed — switched to a recursive scan.\n- A closed bot PR broke the loop without saying anything: the action kept force-pushing updated content to the branch, but since the PR was closed, it never reopened. The content just sat there until I reopened it.\n- The bot PR's checks didn't run at all — because GitHub deliberately won't trigger workflows on PRs opened with the default token, to stop infinite loops. The fix is a scoped personal access token.\n\nEvery one of these became a decision record in the repo. None of them are glamorous, and most aren't in any tutorial — but they're the difference between automation that works in a demo and automation you'd trust on a team. That's the part I'd actually want to read about in someone else's write-up.\n\n## A note on the scanner findings\n\nThe scanners flagged a few things this week — a CloudTrail SNS-topic check, an IAM wildcard, an EIP-attachment check. I didn't quietly dismiss any of them. Each got a line in the decision log: the SNS notification was deferred to the Week 4 detection layer, where alert routing actually belongs; the wildcards were reviewed and accepted, because they live in Deny statements (the strictest scope there is) or in a standard KMS key policy; the EIP check was a false positive, since the IP attaches to a NAT gateway by design. Each call written down. That's the difference between a clean dashboard and an honest one.\n\n## Where this leaves things\n\nThree audit trails in place — API, configuration, network — all KMS-encrypted, all mapped to NIS2 measures, all tested together through one integration test. The add-don't-remove pattern proved itself under a second consumer. And the CI automation is finally something I'd trust, even if getting there was the messy part.\n\nNIS2 Article 21 coverage after Week 2: measures (a), (b), (c), (f), (h), (i) — six of ten.\n\n*Next week: identity. AWS Organizations, Service Control Policies as account-wide guardrails, and IAM Identity Center with MFA — deciding who's actually allowed to touch everything built so far.*\n\nRepo Link: [github.com/olanak/aws-nis2-baseline](https://github.com/olanak/aws-nis2-baseline)\n\n---\n",
      "tags": [
        "AWS",
        "Terraform",
        "CI/CD"
      ],
      "published": true,
      "seriesId": "nis2-landing-zone",
      "part": 2
    },
    {
      "id": "post-1781728594395",
      "title": "Building a NIS2-Compliant AWS Landing Zone - Identity Foundations",
      "date": "2026-06-17",
      "excerpt": "Last week I built the three audit trails — API, configuration, and network — all encrypted, all mapped to NIS2 measures, all streaming somewhere useful. Everything that happens in this account now gets recorded. This week answers the next question: who's actually allowed to make things happen, and what stops them from turning the recording off?\n",
      "content": "*Last week I built the three audit trails — API, configuration, and network — all encrypted, all mapped to NIS2 measures, all streaming somewhere useful. Everything that happens in this account now gets recorded. This week answers the next question: who's actually allowed to make things happen, and what stops them from turning the recording off?*\n\nLogging without access control is half a system. You can prove what happened, but you haven't decided who's permitted to do it, and you've done nothing to stop someone from quietly disabling the logs you just spent a week setting up. NIS2 Article 21 names this directly — (i) access control policies and (j) the use of multi-factor authentication. So Week 3 is the identity layer: AWS Organizations to give the account a governance boundary, Service Control Policies as the guardrails inside it, and IAM Identity Center for how humans actually get access.\n\nIt's also the week the project hit its first real LocalStack wall that I couldn't just buy my way past — and the week a deceptively simple Terraform error taught me something I should have known already.\n\n## What I built this week\n\n- **`modules/organizations`** — an AWS Organization with the full feature set, the SCP policy type enabled, and three organizational units: Workloads, Security, Sandbox.\n- **`modules/scp`** — three Service Control Policies (deny-root, region-lock, protect-logging), attached across all three OUs.\n- **`modules/identity-center`** — two permission sets (Administrator and ReadOnly, both short-lived), a demo group, and an account assignment. This one ended up plan-only, for an honest reason I'll get to.\n\n## The shape of the identity layer\n<img src='/blog/week 3.png' alt='week 3' style='width:100%;border-radius:12px' />\nThe organization is the boundary; the OUs group accounts; the SCPs hang off the OUs as ceilings on what anyone inside can do. Identity Center sits alongside, handling how people actually sign in. Three policies across three OUs is nine attachments, which matters later.\nThe thing to understand about a Service Control Policy is that it can only take away. It never grants permission — it sets the maximum any account in the OU is allowed to have, and IAM operates underneath that ceiling. That makes them the right tool for a small number of absolute rules.\n\nI encoded three:\n\n- **deny-root** blocks the root user from doing anything. The root account shouldn't be used for daily work, and this makes \"shouldn't\" into \"can't.\"\n- **region-lock** denies any operation outside eu-central-1, with carve-outs for the genuinely global services (IAM, Organizations, STS, and a few others) that don't belong to a region. That keeps data in the EU — useful for the residency story a NIS2 reviewer cares about — and it closes the trick where an attacker spins resources up in an unused region precisely because nobody's watching it.\n- **protect-logging** is the one I'm most pleased with. It denies the API calls that would turn off CloudTrail or AWS Config — `StopLogging`, `DeleteTrail`, the Config recorder deletions. This is the policy that ties the whole project together: last week's audit layer is now something even an administrator can't quietly switch off. The logs aren't just on; they're defended.\n\nAll three map to NIS2 (i) access control, and protect-logging reaches back to (b) and (f) by keeping the evidence layer intact.\n\n## What broke (1): for_each and values that don't exist yet\n\nThe SCP module's own tests passed cleanly. Then I wired it into the full composition — SCPs attaching to the OUs that the Organizations module creates — and the integration test threw this:\n\n```\nError: Invalid for_each argument\nthe \"for_each\" map includes keys derived from resource attributes\nthat cannot be determined until apply\n```\n\nHere's what I'd done. The attachment resource looped with `for_each` over a map keyed by OU ID. But the OU IDs come from the Organizations module — they don't exist until that module applies. Terraform builds its plan before anything is created, so it needs to know the full set of `for_each` keys up front. Keys derived from not-yet-created IDs are unknowable at plan time, and Terraform refuses rather than guess.\n\nThe reason the module's own tests passed is that they fed it static fake IDs. Only the integration test, with real apply-time IDs flowing in from another module, exposed the problem — which is exactly what an integration test is for.\n\nThe fix is a genuine Terraform pattern, not a hack. Keys have to be plan-time-known; values are allowed to be unknown. So I changed the module's input from a list of IDs to a map of name-to-ID:\n\n```hcl\n# before: targets = [\"ou-1111\", \"ou-2222\"]   # IDs unknown at plan time\n# after:  targets = { Workloads = \"ou-1111\", Security = \"ou-2222\", ... }\n\nattachments = {\n  for pair in setproduct(keys(local.policies), keys(var.targets)) :\n  \"${pair[0]}:${pair[1]}\" => {\n    policy_id = local.policies[pair[0]]\n    target_id = var.targets[pair[1]]   # the unknown part lives in the value\n  }\n}\n```\n\nNow the keys are built from the OU *names* — static strings I know at plan time — and the apply-time IDs sit safely in the values. The rule I wrote down: if `for_each` complains that something is known only after apply, move the dynamic part out of the keys and into the values.\n\n## What broke (2): Identity Center, and an honest plan-only call\n\nIdentity Center was the module I expected trouble from, so I probed it before building the whole thing — ran the raw API calls against LocalStack to see what actually works. Good news at first: it created the SSO instance, the permission sets, and the identity-store group without complaint. So I built the full module, apply-mode like everything else, and ran it.\n\nIt failed — but not where I expected:\n\n```\nStatusCode: 501\nDescribePermissionSetProvisioningStatus action has not been implemented\n```\n\nThe permission sets create fine. But when you attach a managed policy to one, the AWS provider then polls a provisioning-status endpoint to confirm the change went through — and LocalStack hasn't implemented that endpoint. So the attachment hangs and fails. The create calls I probed all worked; the failure was in a status check that only the full apply reaches. Lesson noted: when you probe a risky service, probe the *whole* graph, including the attachments and whatever happens after them, not just the create.\n\nThen the actual decision. I had three options: code a workaround, drop the managed-policy attachments, or keep the module exactly as it should be and accept it can't fully apply on the emulator. I went with the third — the module is correct Terraform that applies on real AWS, so I kept it whole, marked it plan-only, and left it out of the local composition. It still gets validated and plan-tested in CI; it gets its real apply in Week 6 on actual AWS.\n\nI wrote that up as a decision record, because the distinction matters: this isn't \"my code is broken,\" it's \"the emulator doesn't implement this one endpoint.\" Being able to tell those two apart, and being honest about which one you're looking at, is most of the job. Pretending everything applies cleanly would be the weaker story.\n\n## The refactor I didn't plan on: dev and prod over one composition\n\nWhile closing out the week I noticed the structure was about to become a liability. Everything lived in a single `demo` environment pointed at LocalStack. Week 6 needs a real-AWS target too, and the obvious move — copy the whole module wiring into a second environment — is exactly how environments drift apart. The day they diverge, the project's central claim (\"the same code runs on LocalStack and on real AWS\") quietly stops being true.\n\nSo I split it: the module wiring now lives once, in a shared composition with no provider block of its own. Two thin wrappers call it — `dev` supplies the LocalStack provider and endpoints, `prod` supplies the real-AWS provider and a remote backend. The wiring exists in one place, so the two environments can't drift. The parity is structural now, not something I have to remember to maintain.\n\nThat refactor surfaced one more thing worth keeping: the integration test is a *separate* consumer of the composition, so it needs the full set of LocalStack endpoints too. The first run after the split failed with `UnrecognizedClientException` on half the services — they were falling through to real AWS because the test's provider only declared four endpoints. A small fix, but a clean illustration of why pulling provider config apart from module wiring is the right separation: the gap was suddenly obvious instead of hidden.\n\n## A note on the scanner findings\n\nOne new finding this week, and it's a fair one: Checkov flagged the Administrator permission set for using the AWS `AdministratorAccess` policy. I didn't remove it and I didn't silently ignore it. Every organization needs a break-glass admin path; the control isn't \"never have admin,\" it's how tightly that path is bounded. Mine is bounded four ways — one-hour sessions, MFA-enforced SSO sign-in, assignment to a single group on a single account, and the SCP guardrails sitting above it. I suppressed the check with that reasoning written inline and a matching entry in the decision log. A reviewer sees exactly what was granted and what fences it in.\n\nWhile reconciling the open findings against the log, I also caught one that had slipped through un-triaged — an EIP-attachment warning on the NAT gateway, which is a false positive (the IP attaches to a NAT gateway by design, not an EC2 instance). Now recorded. The point of the decision log is that every open finding has a written answer; an un-triaged one is a small hole in that promise.\n\n## Where this leaves things\n\nThe account now has a governance boundary, three guardrails that hold even against an admin, and a workforce-access model built on short-lived SSO sessions instead of long-lived keys. One module is plan-only and honest about why. The environments are split so the real-AWS run in Week 6 is a thin wrapper away, not a copy-paste job.\n\nNIS2 Article 21 coverage after Week 3: measures (a), (b), (c), (f), (h), (i), (j) — seven of ten.\n\n*Next week: detection. GuardDuty, Security Hub, and the EventBridge-to-SNS alerting pipeline — including the SNS notification I deferred back in Week 2. The point where all these logs finally turn into something that pages you when it matters. I'm expecting GuardDuty and Security Hub to have their own emulation quirks, so the probe-first habit comes with me.*\n\nRepo Link: [github.com/olanak/aws-nis2-baseline](https://github.com/olanak/aws-nis2-baseline)\n\n---\n",
      "tags": [
        "AWS",
        "Terraform",
        "CI/CD"
      ],
      "published": true,
      "seriesId": "nis2-landing-zone",
      "part": 3
    },
    {
      "id": "post-1781964898703",
      "title": "Building a NIS2-Compliant AWS Landing Zone - Detection",
      "date": "2026-06-20",
      "excerpt": "Everything so far has been about evidence and control: logs that can't be switched off, guardrails that can't be bypassed. But a landing zone that only records and restricts is still passive. Week 4 adds the active half — threat detection that generates findings, a place those findings aggregate and get scored, and one channel that turns a finding into a notification.",
      "content": "*Last week I built the governance layer: an organization, three Service Control Policies that hold even against an admin, and short-lived SSO access. The account knows who's allowed to do what. This week is the part where it starts watching — and where it finally pages someone when something looks wrong.*\n\nEverything so far has been about evidence and control: logs that can't be switched off, guardrails that can't be bypassed. But a landing zone that only records and restricts is still passive. Week 4 adds the active half — threat detection that generates findings, a place those findings aggregate and get scored, and one channel that turns a finding into a notification. NIS2 Article 21 calls for (e) vulnerability handling and (g) basic cyber hygiene, and reinforces (b) incident handling. Three measures, give or take, in one week.\n\nI ended last week's post expecting GuardDuty and Security Hub to have \"their own emulation quirks.\" They did — just not the quirk I was bracing for.\n## What I built this week\n\n- **`modules/guardduty`** — a GuardDuty detector with S3 data-event and EBS malware protection, publishing findings every fifteen minutes.\n- **`modules/security-hub`** — Security Hub enabled, subscribed to the AWS Foundational Security Best Practices standard, with GuardDuty wired in as a finding source.\n- **`modules/alerting`** — a KMS-encrypted SNS topic fed by EventBridge rules that match GuardDuty and Security Hub findings. The one place alerts come out.\n\nTwo of those three never ran locally. That's the story of the week, and it's worth being precise about why.\n\n## The shape of the detection layer\n\n<img src='/blog/week 4.png' alt='week 4' style='width:100%;border-radius:12px' />\nGuardDuty consumes the logs from earlier weeks automatically — no wiring, we just enable it. Its findings flow into Security Hub for scoring and into EventBridge for routing. EventBridge matches the findings worth alerting on and drops them into a single encrypted SNS topic. One detector, one scoring surface, one notification channel.\n## A different kind of \"the emulator can't\"\n\nLast week I hit a plan-only module — Identity Center — because LocalStack emulated most of it but returned a 501 on one endpoint the AWS provider polls. That was a *partial* gap, and I only found it mid-apply.\n\nGuardDuty and Security Hub are a different case entirely: LocalStack doesn't implement them at all. Not partially, not with a missing sub-call — there's simply no detector to create and no hub to enable. I checked the coverage before writing a line of apply code, which meant I could make the call up front instead of discovering it in a failed run. Both modules are plan-only from the start: correct, complete Terraform, validated with `terraform validate` and plan-mode tests, never wired into the local composition, with the real apply waiting for the one paid AWS run in Week 6.\n\nI wrote this up as its own decision record, separate from last week's, because the two are genuinely different and the difference is the interesting part. One is \"the emulator emulates this but not that specific poll.\" The other is \"the emulator doesn't know this service exists.\" Both end in plan-only, but the engineering response differs — one you discover by probing, the other you decide by checking coverage first. Naming that distinction is the kind of thing I'd want a reviewer to notice, because it's the difference between flailing at a failed apply and making a deliberate call.\n\nThere's a quiet upside, too. It means the Week 6 real-AWS run isn't a formality — it's the first time the detection layer lights up with authentic findings instead of mocked ones. That run has real weight now.\n\n## The one module that does run, and the chain inside it\n\nThe alerting module is apply-mode, because SNS and EventBridge are both well-covered on LocalStack. I probed all three pieces first — SNS topic, EventBridge rule, KMS key — and they came back with real ARNs, so I built it for real and it composes into the local stack alongside everything from the earlier weeks.\n\nThe part worth slowing down on is encryption. The SNS topic is KMS-encrypted, because everything in this project is. But an encrypted topic that EventBridge needs to publish to has a permission chain that's easy to get subtly wrong. EventBridge needs two separate grants: the SNS topic policy has to allow the EventBridge service to publish, and — the one people miss — the KMS key policy has to let EventBridge generate a data key and decrypt. Without the second grant, publishing fails on real AWS with an error that doesn't obviously point at KMS.\n\nHere's the catch that makes this a good story rather than just a config detail: LocalStack doesn't enforce KMS key policies. So the module applies cleanly locally whether or not I got that second grant right. The test passing tells me the resources exist; it tells me nothing about whether the permission chain actually works. I wrote both grants correctly and then wrote down, explicitly, that this specific path is correct-but-unverifiable locally — it's one of the things the real-AWS run exists to confirm. A green local test that proves less than it appears to is worth being honest about.\n\n## Keeping the pieces from depending on each other\n\nThere's a design decision hiding in how alerting connects to the two plan-only modules: it doesn't. The EventBridge rules match on the *shape* of a finding event — its source (`aws.guardduty`) and its type — not on any resource ID from the GuardDuty or Security Hub modules. So the alerting module composes and applies locally even though the two services feeding it aren't present at all. Match the event pattern, not the producer, and the consumer stops caring whether the producer exists yet. That's what let one apply-mode module sit cleanly alongside two plan-only ones without any awkward conditional wiring.\n\n## Closing two old findings the right way\n\nTwo Checkov findings had been sitting in my decision log since Week 2, both deferred to \"the detection week.\" This was the week, and both closed — but neither closed the way the scanner literally asked.\n\nThe first was CloudTrail not defining an SNS topic. The mechanical fix would be to bolt an SNS topic onto the trail. But that fragments alerting — every log producer ending up with its own notification path. The centralized EventBridge-to-SNS design makes a per-trail topic unnecessary, so I closed the finding as superseded by architecture, with the reasoning written down. The underlying need — get incident notifications out — is met better by the central channel than by what the rule suggested.\n\nThe second was the S3 log bucket lacking event notifications. That one I did implement, because it genuinely belongs: a single line enabling EventBridge notifications on the bucket, so object activity flows into the same routing layer as everything else. One finding closed by saying \"the architecture already handles this, here's how,\" the other by \"yes, that's a real gap, fixed.\" Both got a written disposition. That, more than a green dashboard, is what a decision log is for.\n\n## Where this leaves things\n\nThe landing zone can now watch itself and speak up. GuardDuty and Security Hub are built and correct, waiting on real AWS to prove they apply. The alerting backbone runs locally today, encrypted, with its permission chain written correctly and honestly flagged as the thing the real run will confirm. Every finding in the decision log has an answer next to it.\n\nNIS2 Article 21 coverage after Week 4: measures (a), (b), (c), (e), (f), (g), (h), (i), (j) — nine of ten. The only one left is (d), supply chain security, and it isn't really a module — it's an architecture-and-documentation question about dependencies, pinning, and provenance. Which is a good lead-in to what's next.\n\n*The remaining two weeks change character. Week 5 is consolidation — the NIS2-to-ISO crosswalk, the supply-chain write-up that closes the last measure, the architecture docs pulled together into something a reviewer can read top to bottom. Week 6 is the one paid AWS run: the moment all the plan-only modules and the unverifiable permission chains finally meet real infrastructure, plus the capstone post. The build is nearly done; what's left is proving it and explaining it.*\n\nRepo Link: [github.com/olanak/aws-nis2-baseline](https://github.com/olanak/aws-nis2-baseline)\n\n---",
      "tags": [
        "AWS",
        "Terraform",
        "CI/CD"
      ],
      "published": true,
      "seriesId": "nis2-landing-zone",
      "part": 4
    }
  ]
};
